---
name: Open questions
description: Open escalations for the ExpenseMate Website that need human, product, or cross-project input
type: project
---

# Website — open questions

One bullet per question. Mark resolved in place with `[x]` and a strikethrough,
linking to the ADR if the resolution warrants one.

- [x] ~~2026-04-29 — Should `public/downloads/` track an Android `v1.3.x` entry, or is Android paused at `v1.2.5` (last entry: 2025-11-14) until the next cut? Needed before any v1.4 downloads page work.~~ Resolved 2026-04-30 — moot. The marketing site no longer advertises the APK route at all (closed-beta only); no `v1.3.x` entry needed before Play Store launch.
- [x] ~~2026-04-29 — Confirm the canonical production domain. `AGENTS.md` references `https://expensemate.app` for deep-link compatibility; no `metadataBase` or hard-coded host is set in the repo, so deploy-time configuration (Vercel project domain, env) is the source of truth.~~ Resolved 2026-04-30 — confirmed `https://expensemate.app`.
- [x] ~~2026-04-29 — `next.config.ts` wires `Content-Type: application/json` headers for `/.well-known/apple-app-site-association` and `/.well-known/assetlinks.json`, but only `assetlinks.json` has a route handler reading from `public/.well-known/`. Is `apple-app-site-association` served as a static file from `public/.well-known/`, or is it missing?~~ Resolved 2026-05-13 — file exists at `public/.well-known/apple-app-site-association`; no route handler is needed for the static file path.
- [x] ~~2026-04-29 — Home page First Load JS sits at ~222 kB after the ambient Remotion pieces. Acceptable per `CHANGELOG.md`, but no explicit budget is recorded. What is the threshold at which we defer the Player via dynamic import + intersection observer?~~ Resolved 2026-04-30 — moot. Remotion has been stripped entirely; First Load JS is now ~161 kB and the Player runtime is no longer in the bundle.
- [x] ~~2026-04-30 — PostHog is now live and is the main website product/funnel analytics source. Should Google Analytics remain in production long-term for legacy comparison, or should GA be removed/downgraded to reduce duplicate analytics tooling?~~ Resolved 2026-05-13 — keep Google Analytics alongside PostHog for now.
- [ ] 2026-05-13 — PostHog session replay is disabled by default in repo code unless `NEXT_PUBLIC_POSTHOG_SESSION_REPLAY=true` is set in deployment. If production has enabled that env var, confirm explicitly whether replay should stay on; otherwise no repo action is needed.
- [ ] 2026-08-04 — **When does the `/join` 307 go live?** It is committed but must not deploy before the web app is live at `app.expensemate.app`, or every invite link in the wild breaks. The two repos deploy independently, so nothing in the code enforces this — it needs a human to sequence the two Vercel deploys (web app first, marketing site second). Confirm the ordering is understood before the next marketing-site deploy for any *unrelated* reason, since that deploy would ship the redirect with it. (blocks nothing today; **breaks production if ignored**)
- [ ] 2026-08-04 — **`/join` is `Disallow`ed for assistant crawlers in `robots.ts` and is now a 307.** Harmless, but worth a decision: the disallow was written for a page that rendered content. Leaving it is fine and arguably still right (an invite link is not a page anyone should be indexing). No action unless someone wants join links to be followable. (blocks nothing)
- [ ] 2026-08-04 — **Which region is the PostHog project actually on?** `src/utils/analytics.ts:91` defaults to `https://eu.i.posthog.com`, and the privacy policy's processor table says "EU Cloud (Frankfurt)" — but the 2026-04-30 STATUS entry records production and preview being set to `NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com`, which would override the default. Both cannot be true. **If production really is on US Cloud, the privacy policy is wrong on a transfer question** and the web app must not reuse that project without saying so. Check the host in the Vercel project env and the region on the PostHog project itself. (**blocks nothing today; a live legal inaccuracy if the US value is set**)
- [ ] 2026-08-04 — **What retention is configured on the Sentry project?** The privacy policy now says error reports are "retained for the retention period configured on our Sentry project" rather than naming a number, because the number could not be verified from any repo. Once the project exists, put the real figure in `src/app/privacy/page.tsx` §10. (blocks nothing)
- [ ] 2026-08-04 — **Three PostHog events are now unreachable from this site**: `join_page_viewed`, and the app-open events the deleted `AppRedirect` fired. They remain declared in `src/utils/analytics.ts`. Decide with the web-app Phase 5 analytics work whether the web app re-emits `join_page_viewed` under the same name to keep the historical series continuous, or whether the union names get retired here. (blocks nothing; **product/analytics decision**)
