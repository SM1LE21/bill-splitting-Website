---
name: Website status
description: Current focus, recent changes, blockers, next steps, and runtime facts for the ExpenseMate Website
type: project
---

# Website — STATUS

Last reviewed: 2026-08-04

## Current focus

- Marketing reset shipped: a new `ProductFlow` section now anchors the home page with three React-built iOS screens (Snap → Split → Settle) running framer-motion-driven animation, Features section rebuilt with real copy and stroke icons, SocialProof strip under the Hero, iOS-only Hero CTA shortcut to the App Store, FAQ leads with the receipt-accuracy question.
- Analytics: PostHog is live on production and is now the primary website product/funnel analytics source. Vercel Analytics remains the always-on cookieless traffic baseline; Google Analytics remains consent-gated legacy analytics by owner decision on 2026-05-13.
- Roadmap: Group Reports v1.4 flipped to "Completed" (Released May 2026), now lives behind the "Show completed milestones" toggle. Android v1.0 still reads "Beta — Play Store soon" with the delayed badge retained. Web / Personal Space / Work Space milestones unchanged.
- Public-facing release notes track iOS through 1.4.2 (released 30th May 2026, Copy Group ID + group-invite-link join fix). 1.4.1 (13th May 2026) and 1.4.0 (5th May 2026) entries remain below it. The `/downloads` Android APK route is intentionally not advertised on the home page — closed-beta only until the Play Store launch.

## Recent changes

- 2026-08-04 — **`/join` is now a 307 to the web app, and the AASA `appID` case is fixed.** ExpenseMate Web Phase 4; parent `.vault/briefs/WEB-v1-build-spec.md` §14.
  - **⚠️ DEPLOYMENT ORDERING — BINDING. The `/join` redirect must NOT go live before the web app is live at `app.expensemate.app`.** Until then this route sends every invite link in the wild to a host that does not resolve. The invite URL `https://expensemate.app/join?groupId=<UUID>` is built in iOS 1.4.2 and 1.5.0 and is already sitting in users' message threads. **This ships in the same release as the web app (web spec Phase 6), not before.** If the web app slips, revert `cba3fa3` — the join UI it replaces is recoverable from `3a86eeb`/`2da4447`.
  - `public/.well-known/apple-app-site-association`: `appID` `87U8WV4Q3F.app.expensemate` → **`87U8WV4Q3F.app.ExpenseMate`**. Verified against `Frontend`, not assumed: `PRODUCT_BUNDLE_IDENTIFIER = app.ExpenseMate` and `DEVELOPMENT_TEAM = 87U8WV4Q3F` in `BillSplitApp.xcodeproj/project.pbxproj:1245`/`:1224`, confirmed by `App/GoogleService-Info.plist`. Apple matches `appID` case-sensitively, so the association never validated — which is why 1.4.2 had to fall back to `expensemate://`. The lowercase string was `CFBundleURLName` (`BillSplitApp-Info.plist:22`), the label on the custom URL scheme; it is not the bundle ID and does not have to match one. Same typo, same product, as the APNs `TopicDisallowed` incident.
  - `src/app/join/page.tsx` renders nothing now: it 307s to `https://app.expensemate.app/join` with the query string carried through untouched. Nothing is validated — missing, empty, duplicated and malformed `groupId` values all redirect, and the web app owns the "invite link isn't valid" state so there is one copy of it. Verified against `next start`: 307 + exact `Location` for seven query shapes, no 500.
  - `JoinGroup.tsx` and `AppRedirect.tsx` deleted (240 lines). They existed to hand-roll the app open that Universal Links now do natively. `src/utils/deviceUtils.ts` keeps its helpers (`attemptAppRedirect`, `isValidUUID`, link builders) — they have no caller on this route any more but the module has other exports.
  - **Content type verified locally**: `/.well-known/apple-app-site-association` → `200`, `content-type: application/json`, no redirect. The `next.config.ts` `headers()` rule does apply to the static file in `public/`; no route handler is needed (unlike `assetlinks.json`, which has one).
  - **The AASA fix is inert until the apex→www 308 is reversed** — see Blockers. That is owner action in the Vercel dashboard and it is now the single thing standing between this repo and working Universal Links.
- 2026-07-31 — **Comparison pages drafted, not published.** `/vs-splitwise` and `/export-expenses-excel` exist in the repo as `noindex, nofollow`, are absent from `sitemap.ts`, and carry no nav or footer link. **To publish, once Apple approves iOS 1.5.0: (1) remove the `robots` line from each page's `metadata`, (2) add both routes to `src/app/sitemap.ts`.** They stay orphaned by design — owner decision 2026-07-31: reachable by search, not by browsing, on the main domain rather than a separate site (a one-page satellite domain would start with no authority, reads as a doorway site, and would fragment the entity graph the JSON-LD work just consolidated). Sourcing rule for these pages: Splitwise claims cite Splitwise-owned pages or Apple only, never third-party blogs; `src/constants/comparisonSources.ts` holds the citation list and the `CHECKED_ON` date, which must be re-verified whenever the pages change. **Open question:** whether Splitwise gates CSV export could not be established from first-party sources, so neither page claims anything about it.
- 2026-07-31 — **Retrieval foundation shipped (Phase 1 of the discovery work).** First `robots.txt` and `sitemap.xml` on the site (`src/app/robots.ts`, `src/app/sitemap.ts`); assistant crawlers explicitly allowed, `/downloads` + `/join` disallowed for them. `/downloads` set to `noindex, follow` — it advertises Android alpha APKs while the shipping product is iOS-only, and search answers were free to conclude ExpenseMate ships on Android. Created the OG/Twitter card images, which had been referenced in `layout.tsx` and `StructuredData.tsx` since launch but never existed (both 404'd in production, so every preview card was broken). Structured data now carries `WebSite` + `FAQPage` alongside `Organization`/`SoftwareApplication`, a corrected App Store URL, and a self-referencing canonical per route. **Open, owner-action:** the apex→www 308 must be reversed in Vercel before any of this points at a non-redirecting host — see Blockers.
- 2026-06-24 — **Premium launch website prep** (part of the cross-repo monetization launch; see parent `.vault/briefs/BATCH-B-go-live-handoff.md`). Added a `Premium` homepage section (`src/components/sections/Premium.tsx`, placed after `Benefits`) with the free-vs-Premium comparison + €2.99/mo & €24.99/yr cards. Added the iOS 1.5.0 release-notes entry (Premium; early users keep Premium free). Updated the Privacy Policy for in-app PostHog product analytics + Apple/RevenueCat subscription sub-processors (new Subscription Data category, legal-basis + retention rows, "Last updated" → June 24 2026). Lint + build verified. **Lockstep note:** the Premium section + 1.5.0 release notes should go live with the App Store release; the privacy-policy update is fine to be live earlier (App Review checks it). Privacy text must stay consistent with the ASC App Privacy answers.
- 2026-05-30 — iOS 1.4.2 entry added to `src/components/sections/ReleaseNotes.tsx` (released 30th May 2026): Copy Group ID (share a group by its raw ID from the share menu / settings) and a bugfix for group-invite links opening the app without landing on the join screen. No roadmap changes — 1.4.2 is a small feature + fix on the v1.4 line.
- 2026-05-13 — iOS 1.4.1 entry added to `src/components/sections/ReleaseNotes.tsx` (released 13th May 2026): Home Screen quick actions (long-press → Scan Receipt or Add Expense with a group picker) and auto-detected transaction date from scanned receipts. No roadmap changes — 1.4.1 is a feature follow-up to the v1.4 card already marked Completed.
- 2026-05-13 — Documentation triage: confirmed `public/.well-known/apple-app-site-association` exists and is served as a static public file with `next.config.ts` content-type headers; no route-handler fix needed. Google Analytics remains alongside PostHog for legacy comparison.
- 2026-05-05 — iOS 1.4.0 published on the website. Added the 1.4.0 entry to `ReleaseNotes.tsx` (Group Reports PDF/CSV with period selection and share-sheet handoff, Reports toolbar shortcut on the Summary tab, Insights pivoted to a personal "Your share / You paid / Net" view with Highlights, back-date on Add Expense, pull-to-refresh + skeleton loading, iOS Passwords / iCloud Keychain autofill on Login + Sign-up, plus the PDF gray-on-white rendering fix). Flipped the "Q2 2026 — Version 1.4 Launch — Group Reports (iOS)" Roadmap card from `shipping` to `completed` with date "Released May 2026".
- 2026-04-30 — PostHog production ingestion verified: a fresh incognito session accepting analytics cookies sends events into the PostHog US Cloud project. Production/preview Vercel env must use `NEXT_PUBLIC_POSTHOG_KEY` plus `NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com`; keep the `phc_...` token out of repo docs. PostHog is the main website product analytics layer for funnels such as `page_viewed -> cta_clicked -> app_store_clicked`, join-link health, download clicks, and app-open fallbacks.
- 2026-04-30 — Benefits section rebuilt as an auto-cycling four-scene framer-motion reel. Same four messages (Save time / Avoid conflicts / Stay organised / Peace of mind) on a ~28-second loop with cross-fades, per-scene animated visuals, IntersectionObserver-driven auto-pause when off-screen, hover-pause on desktop, dot navigation, and a `prefers-reduced-motion` fallback that locks scene 1 in place. No new dependencies. Home First Load JS ~168 kB.
- 2026-04-30 — Product flow narrative replaces the App Store screenshot gallery. Built `PhoneFrame` shell + three React/Tailwind iOS screens (Scan, Split, Settle) under `src/components/sections/ProductFlow*.tsx` with continuous in-screen motion (scan beam, avatar cycle, count-up balances, settle-button pulse). Deleted `AppScreenshots.tsx` and the six App Store PNGs. Decision recorded in [ADR 0005](adr/0005-build-product-screens-as-react-components.md), which supersedes 0001 and 0003.
- 2026-04-30 — PostHog website analytics prepared in code: `posthog-js` is lazy-loaded only after analytics consent and `NEXT_PUBLIC_POSTHOG_KEY` is present, tracks core website funnels, and scrubs URL query strings before sending events so `/join?groupId=...` values are not sent.
- 2026-04-30 — Marketing pass landed (single session, eight micro-commits): Features rebuild, AppScreenshots gallery (six App Store screenshots under `public/images/screenshots/`), SocialProof strip, Hero CTA device routing, FAQ accuracy entry, Roadmap refresh, full Remotion strip.
- 2026-04-30 — Remotion gone: removed `@remotion/player` embeds from HowItWorks/Benefits/CTA, deleted `ReceiptDemo.tsx` and `src/remotion/`, dropped `remotion` + `@remotion/player` from `package.json`, removed the `remotion-best-practices` skill (gone from `.claude/skills/` and `skills-lock.json`). Home First Load JS down from ~222 kB to 161 kB.
- 2026-04-25 — Three ambient Remotion backgrounds, FeaturesShowcase v3/v4, ReceiptDemo, Roadmap v1.4 rescope, ReleaseNotes 1.3.1, Remotion setup. (See `CHANGELOG.md` for detail; all of this was reverted on 2026-04-30 except the rescoped v1.4 card and the 1.3.1 release-notes entry.)

## Blockers

- **apex→www 308 (owner action, Vercel dashboard).** `expensemate.app` 308-redirects to `www.expensemate.app`, but every canonical the site publishes — `metadataBase`, `openGraph.url`, `StructuredData` `SITE_URL`, `robots.ts`, `sitemap.ts` — declares the apex. So the declared canonical host is the one that redirects away. The same 308 breaks Universal Links outright: the iOS entitlement is `applinks:expensemate.app` (apex) and `https://expensemate.app/.well-known/apple-app-site-association` returns 308, which iOS does not follow (verified 2026-07-31; the www host serves it 200). Fix direction is therefore apex-primary, www-redirects-to-apex — not the reverse. Until this is flipped, sitemap URLs and canonicals point at a redirect and Universal Links stay broken.
  - **Elevated 2026-08-04: this is now the only thing blocking Universal Links from this repo's side.** The `appID` case bug — the other half — is fixed. Exact owner steps, Vercel dashboard → the **marketing site** project → Settings → Domains: set **`expensemate.app` as the primary/production domain** and make `www.expensemate.app` the one that redirects to it. Vercel generates the 308 automatically from whichever domain is *not* primary, so this is a toggle, not a rewrite rule; do not try to fix it with a `redirects()` entry in `next.config.ts`, which cannot remove a platform-level redirect.
  - **Verify after flipping, from outside the Vercel edge:** `curl -sSI https://expensemate.app/.well-known/apple-app-site-association` must return **`200`** with **`content-type: application/json`** and **no `Location` header**. A `301`/`302`/`307`/`308` at that URL means Universal Links are still dead — Apple's CDN does not follow redirects when fetching the association.
  - **Then allow for propagation.** The association is fetched by Apple's CDN and delivered to devices on app install/update and periodically after; expect up to ~24h before a device that already has 1.5.0 starts opening `/join` links in the app. A device test the same afternoon proving nothing is not evidence the fix failed. To test immediately, install a development build whose entitlement uses `applinks:expensemate.app?mode=developer`, which bypasses the CDN.
  - **`applinks:expensemate.app` does not cover `www.expensemate.app`.** Any invite that reaches a user on the www host will not open the app even after the flip. Another reason the apex must be primary.
- No current blocker for PostHog website ingestion. If events disappear, first verify browser consent state, ad blockers/privacy extensions, the Vercel `NEXT_PUBLIC_POSTHOG_KEY`, the US Cloud host override, and that the PostHog Activity view is not filtering out internal/test users.

## Next steps

- Build the first PostHog dashboard: traffic by path/referrer/device, App Store click funnel, download clicks by version, join-link validity, app-open fallback rate, and top UTM sources.
- Create saved PostHog funnels for `page_viewed -> cta_clicked -> app_store_clicked` and `/downloads page_viewed -> download_clicked`, with breakdowns by `device_type`, `referrer_host`, and UTM properties.
- **The `join_page_viewed -> app_open_attempted -> app_open_failed` funnel is dead as of 2026-08-04** and should not be built. `/join` no longer renders anything, so none of those three events can fire from this site; the event names remain declared in `src/utils/analytics.ts` but are unreachable. Join-flow analytics move to the web app in its Phase 5. Decide there whether `join_page_viewed` keeps its name for continuity with the historical series — and keep the rule that no `groupId` value is ever sent as an event property.
- Keep PostHog session replay disabled by default unless `NEXT_PUBLIC_POSTHOG_SESSION_REPLAY=true` is intentionally set in deployment. If replay is enabled, treat it as a privacy/product decision even though text/attributes are masked.
- Roadmap card flip + 1.4.0 release notes done on 2026-05-05. Still open: consider whether `ProductFlow` should grow a fourth phone for Group Reports (per ADR 0005, this is a single-file extension).
- When Android hits the Play Store: swap the Roadmap card status off "delayed", re-enable the Google Play CTA in `CTA.tsx` (currently a disabled visual), and decide whether `ReleaseNotes.tsx` should track Android entries (currently iOS-only by convention — see `feedback_release_notes_scope` memory).

## Runtime / deployment

- Framework: Next.js 15.3.6, App Router, React 19, TypeScript, Tailwind 3.4. Dev script runs `next lint` before `next dev --turbopack`.
- Hosting: Vercel. Canonical production domain confirmed as `https://expensemate.app` (2026-04-30).
- Analytics: `@vercel/analytics` remains always-on/cookieless. Google Analytics remains consent-gated legacy analytics by owner decision on 2026-05-13. PostHog is the primary consent-gated website product/funnel analytics layer (`src/utils/analytics.ts`, `src/components/ui/PostHogAnalytics.tsx`), lazy-loaded after consent and Vercel env config; the live project is US Cloud, so Vercel must set `NEXT_PUBLIC_POSTHOG_HOST=https://us.i.posthog.com`. Event properties intentionally avoid raw join `groupId` values and scrub URL query strings via `before_send`. Session replay is disabled by default in code unless `NEXT_PUBLIC_POSTHOG_SESSION_REPLAY=true` is set.
- Downloads: Android APKs hosted on GitHub Releases under `SM1LE21/bill-splitting-Website` (see `public/downloads/v*/metadata.json`). Local APKs are git-ignored (`*.apk`). Closed-beta route only — not advertised in marketing.
- Deep links: `/.well-known/assetlinks.json` (Android) served via a route handler that reads `public/.well-known/assetlinks.json`. Apple App Site Association is the static file in `public/.well-known/`, with its `application/json` content type wired in `next.config.ts` (verified serving 200 + `application/json`, no redirect). `appID` is `87U8WV4Q3F.app.ExpenseMate` and the case is load-bearing. `/join?groupId=<uuid>` is the integration point with mobile and its format is fixed by shipped iOS builds — query parameter, not path segment.
- **Serving an AASA from `app.expensemate.app` (web spec §14.2, "second host") is currently inert.** The iOS entitlement lists only `applinks:expensemate.app` (`Frontend/BillSplitApp/BillSplitApp.entitlements`), so iOS will not consult an association on any other host. Adding `applinks:app.expensemate.app` is a `Frontend` change that ships with a future iOS build; the file on the web-app host is staged ahead of it. Note also that a Universal Link **does not fire through the 307** — iOS only evaluates the URL the user actually tapped — so the association on this apex domain is the one that matters for invite links.
- Bundle: Home First Load JS ~168 kB after the Benefits reel; was ~166 kB after the PostHog wrapper + ProductFlow section, ~161 kB right after the Remotion strip, ~222 kB before the Remotion strip. PostHog itself lazy-loads after consent.
- This folder is a Git submodule of `Bill-Splitting-App`. Commit website changes here first, then update the parent repo pointer if needed.
