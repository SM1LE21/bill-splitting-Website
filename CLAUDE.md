# CLAUDE.md

This repo's canonical agent instructions live in [`AGENTS.md`](AGENTS.md).

This file exists as a compatibility pointer for tools or older docs that still
look for `CLAUDE.md`. Do not duplicate guidance here; update `AGENTS.md` and the
repo-owned `.vault/` files instead.
